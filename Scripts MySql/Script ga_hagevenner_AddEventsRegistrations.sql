SELECT * FROM ga_hagevenner.eventregistrations;

insert into eventregistrations(EventId, MemberId, Status, Created, Updated)

values(2,
		"2", 
		"Confirmed",        
        now(),
        now()
        );

insert into eventregistrations(EventId, MemberId, Status, Created, Updated)

values(1,
		"2", 
		"Confirmed",        
        now(),
        now()
        );